<%= form_with(model: jobtitle, local: true) do |form| %>
  <% if jobtitle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jobtitle.errors.count, "error") %> prohibited this jobtitle from being saved:</h2>

      <ul>
      <% jobtitle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
